export const toastDuration = 1000

export const CSRF_TOKEN = 'x-csrf-token'

export const lastMessageDateFormat = {
	sameDay: 'HH:mm ',
	sameElse: 'DD/MM/YYYY',
}

export const dateFormat = {
	sameDay: 'HH:mm',
	sameElse: 'DD/MM/YYYY HH:mm',
}
